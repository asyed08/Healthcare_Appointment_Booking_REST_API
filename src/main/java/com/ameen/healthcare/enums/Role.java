package com.ameen.healthcare.enums;

/**
 * Defines the roles a registered user can hold in the system.
 * Used by Spring Security for role-based access control.
 */
public enum Role {
    PATIENT,
    DOCTOR
}

